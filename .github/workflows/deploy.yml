name: Deploy Angular site to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: aiRA-multilingual/package-lock.json

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        working-directory: aiRA-multilingual
        run: npm ci

      - name: Build Angular application
        working-directory: aiRA-multilingual
        run: npm run build -- --base-href=/aiRA/

      - name: Prepare Pages artifact
        run: |
          INDEX_FILE=$(find aiRA-multilingual/dist -type f -name index.html | head -n 1)

          if [ -z "$INDEX_FILE" ]; then
            echo "No index.html found in aiRA-multilingual/dist"
            exit 1
          fi

          SITE_DIR=$(dirname "$INDEX_FILE")

          mkdir -p pages-dist
          cp -r "$SITE_DIR"/. pages-dist/
          touch pages-dist/.nojekyll

          echo "Publishing files from $SITE_DIR"
          find pages-dist -maxdepth 2 -type f | head -50

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: pages-dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
